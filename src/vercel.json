{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps --ignore-scripts && npm run postinstall",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
